spring:
  application:
    name: identity-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5433/social_network_identity"
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: redis-14597.c291.ap-southeast-2-1.ec2.cloud.redislabs.com
      port: 14597
      password: YnttLGa38uDs29MUZ132smcVDv4Icfzn
      username: default
      ssl:
        enabled: false

server:
  port: 8080
  servlet:
    context-path: /identity

app:
  services:
    profile: http://localhost:8081/profile/info
    notify: http://localhost:8082/notification